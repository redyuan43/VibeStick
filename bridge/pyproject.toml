[project]
name = "vibestick"
version = "0.1.4"
description = "Local VibeStick Bridge service for VibeStick."
requires-python = ">=3.11"
dependencies = [
    "zeroconf>=0.131",
]

[project.scripts]
vibestick-bridge = "vibe_stick.__main__:main"
