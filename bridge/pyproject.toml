[project]
name = "vibestick"
version = "0.1.4"
description = "Battery telemetry service and CLI for VibeStick."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
vibestick-battery = "vibe_stick.telemetry.cli:main"
vibestick-telemetry = "vibe_stick.telemetry.server:main"
