<svg id="cloudling" xmlns="http://www.w3.org/2000/svg" width="880" height="720" viewBox="-32 -24 88 72" preserveAspectRatio="xMidYMid meet">
<style><![CDATA[
.edge-rim-tight { stroke-opacity: .08; }
.edge-rim-soft { stroke-opacity: .06; }
.edge-contact { stroke-opacity: .08; }
@media (prefers-color-scheme: light) {
.edge-rim-tight { stroke-opacity: .15; }
.edge-rim-soft { stroke-opacity: .10; }
.edge-contact { stroke-opacity: .18; }
.cloud-drop-shadow { flood-opacity: .13; }
.food-cloud-shadow { flood-opacity: .28; flood-color: #5060FF; }
.food-cloud > path {
  stroke: #5870E8 !important;
  stroke-width: 1.2 !important;
  stroke-opacity: .9 !important;
  vector-effect: non-scaling-stroke !important;
}
}
]]></style>
    <defs>
      <linearGradient id="body-grad" x1="12" y1="2" x2="12" y2="22" gradientUnits="userSpaceOnUse">
        <stop offset="0" stop-color="#fff8e8"/>
        <stop offset="0.5" stop-color="#ffe9bd"/>
        <stop offset="1" stop-color="#e4bd84"/>
      </linearGradient>
      <linearGradient id="eye-grad" x1="12" y1="3" x2="12" y2="21" gradientUnits="userSpaceOnUse">
        <stop offset="0" stop-color="#20170F"/>
        <stop offset="0.58" stop-color="#110C08"/>
        <stop offset="1" stop-color="#050302"/>
      </linearGradient>
      <radialGradient id="hi-grad" cx="0.45" cy="0.25" r="0.55">
        <stop offset="0" stop-color="#ffffff" stop-opacity="0.9"/>
        <stop offset="1" stop-color="#ffffff" stop-opacity="0"/>
      </radialGradient>
      <filter id="drop" x="-40%" y="-40%" width="180%" height="180%">
        <feDropShadow class="cloud-drop-shadow" id="drop-shadow" dx="0" dy="0.6" stdDeviation="0.8" flood-color="#a36d36" flood-opacity="0.18"/>
      </filter>
      <filter id="edge-blur" x="-40%" y="-40%" width="180%" height="180%">
        <feGaussianBlur stdDeviation="0.16"/>
      </filter>
      <path id="cloud-shape" d="M3.25 20.45 C3.18 14.75 4.72 9.58 7.72 6.72 C8.72 5.76 9.92 5.12 11.18 4.80 C11.10 3.12 11.78 1.82 12.88 1.18 C13.78 0.66 14.88 0.78 15.42 1.20 C14.55 1.86 14.25 2.78 14.52 3.42 C14.88 4.26 15.95 4.28 16.74 3.42 C17.08 4.78 16.55 6.12 15.42 6.78 C18.45 8.05 20.75 13.05 20.75 20.45 C20.75 21.00 20.35 21.45 19.78 21.45 L4.22 21.45 C3.65 21.45 3.25 21.00 3.25 20.45 Z"/>
      <clipPath id="cloud-clip">
        <use href="#cloud-shape"/>
      </clipPath>
    </defs>

    <!-- 云朵 layer (rotate + breath scale + distance scale, 都围绕 12,12) -->
    <g id="cloud-group">
      <g id="edge-system">
        <use class="edge-contact" href="#cloud-shape" fill="none" stroke="#7d8aa7" stroke-width="1.05" stroke-linejoin="round" transform="translate(0 0.18)" filter="url(#edge-blur)"/>
        <use class="edge-rim-soft" href="#cloud-shape" fill="none" stroke="#d3a96e" stroke-width="0.90" stroke-linejoin="round" filter="url(#edge-blur)"/>
        <use class="edge-rim-tight" href="#cloud-shape" fill="none" stroke="#c49c68" stroke-width="0.42" stroke-linejoin="round"/>
      </g>
      <g filter="url(#drop)">
        <use href="#cloud-shape" fill="url(#body-grad)"/>
        <rect x="1" y="2" width="22" height="11" fill="url(#hi-grad)" clip-path="url(#cloud-clip)"/>
      </g>
    </g>

    <!-- 眼睛 layer (独立, 1:1 跟鼠标) -->
    <g id="eye-group">
      <rect id="eye-l" x="7.7" y="9.5" width="2.6" height="5" rx="1.3" ry="1.3" fill="url(#eye-grad)" stroke="#ffffff" stroke-width="0.08" stroke-opacity="0.7"/>
      <rect id="eye-r" x="13.7" y="9.5" width="2.6" height="5" rx="1.3" ry="1.3" fill="url(#eye-grad)" stroke="#ffffff" stroke-width="0.08" stroke-opacity="0.7"/>
    </g>
      <g id="__wowotou-face" pointer-events="none">
        <ellipse cx="8.25" cy="11.55" rx="1.95" ry="2.55" fill="#21170f" opacity="0.98"/>
        <ellipse cx="15.75" cy="11.55" rx="1.95" ry="2.55" fill="#21170f" opacity="0.98"/>
        <circle cx="7.55" cy="10.55" r="0.70" fill="#ffffff" opacity="0.92"/>
        <circle cx="15.05" cy="10.55" r="0.70" fill="#ffffff" opacity="0.92"/>
        <circle cx="8.35" cy="12.95" r="0.42" fill="#ffffff" opacity="0.86"/>
        <circle cx="15.85" cy="12.95" r="0.42" fill="#ffffff" opacity="0.86"/>
        <ellipse cx="5.80" cy="14.75" rx="1.50" ry="0.70" fill="#ff9fc2" opacity="0.34"/>
        <ellipse cx="18.20" cy="14.75" rx="1.50" ry="0.70" fill="#ff9fc2" opacity="0.34"/>
        <path d="M4.55 13.15 L2.85 12.92 M4.55 14.25 L2.72 14.32 M4.66 15.30 L3.05 15.78" stroke="#3a241b" stroke-width="0.32" stroke-linecap="round" opacity="0.82"/>
        <path d="M19.45 13.15 L21.15 12.92 M19.45 14.25 L21.28 14.32 M19.34 15.30 L20.95 15.78" stroke="#3a241b" stroke-width="0.32" stroke-linecap="round" opacity="0.82"/>
        <path d="M17.35 15.95 C16.72 15.18 15.55 15.56 15.56 16.55 C15.56 17.48 16.60 18.16 17.35 18.76 C18.10 18.16 19.14 17.48 19.14 16.55 C19.15 15.56 17.98 15.18 17.35 15.95 Z" fill="#ffc1d5" stroke="#3a241b" stroke-width="0.18" opacity="0.95"/>
        <g filter="url(#drop)">
          <path d="M17.75 6.05 L18.38 7.35 L19.82 7.55 L18.78 8.55 L19.03 9.98 L17.75 9.30 L16.47 9.98 L16.72 8.55 L15.68 7.55 L17.12 7.35 Z" fill="#ffd86e" stroke="#fff3bd" stroke-width="0.18" stroke-linejoin="round"/>
        </g>
      </g>

<script type="application/ecmascript"><![CDATA[
const FACE_CENTER = { x: 12, y: 12 };

const svgEl = document.getElementById('cloudling');
const cloudGroup = document.getElementById('cloud-group');
const eyeGroup = document.getElementById('eye-group');
const eyeL = document.getElementById('eye-l');
const eyeR = document.getElementById('eye-r');
const eyeGrad = document.getElementById('eye-grad');

const EYE_BASE = {
  l: { x: 7.7, y: 9.5, h: 5, rx: 1.3 },
  r: { x: 13.7, y: 9.5, h: 5, rx: 1.3 }
};

const P = {
  /* V1 锁定数值 */
  SCALE_LOW: 0.96, BREATH_PERIOD: 5.0,
  MAX_ROT_DEG: 20, CLOUD_LAG: 0.20,
  EYE_MAX: 1.20, EYE_TRACK_TIME: 0.06, RECENTER_TIME: 0.45,
  FLOW_PERIOD: 5.5, FLOW_AMP: 4,
  BLINK_INTERVAL: 6.0, BLINK_DUR: 0.16,
  /* V2 新增 */
  DIST_NEAR: 6, DIST_FAR: 20, DIST_MAX_SCALE: 1.15, DIST_RECENTER: 0.5
};

let mouseInside = false;
let mouseVB = { x: 12, y: 12 };
let eyeOffset = { x: 0, y: 0 };
let cloudRotate = 0;
let curDistScale = 1.0;
let lastT = performance.now();

let nextBlinkT = 3 + Math.random() * 2;
let blinkActive = false;
let blinkStartT = 0;

svgEl.addEventListener('mousemove', e => {
  const r = svgEl.getBoundingClientRect();
  /* viewBox -12 -12 48 48 → 反算 mouseVB */
  mouseVB.x = (e.clientX - r.left) * (48 / r.width) - 12;
  mouseVB.y = (e.clientY - r.top) * (48 / r.height) - 12;
  mouseInside = true;
});
svgEl.addEventListener('mouseleave', () => mouseInside = false);
svgEl.addEventListener('mouseenter', () => mouseInside = true);

window.__cloudlingSetPointer = payload => {
  if (!payload || !Number.isFinite(payload.x) || !Number.isFinite(payload.y)) return;
  mouseVB.x = payload.x;
  mouseVB.y = payload.y;
  mouseInside = !!payload.inside;
};

function computeEyeTarget() {
  if (!mouseInside) return { x: 0, y: 0 };
  const dx = mouseVB.x - FACE_CENTER.x;
  const dy = mouseVB.y - FACE_CENTER.y;
  const dist = Math.hypot(dx, dy);
  if (dist < 0.01) return { x: 0, y: 0 };
  const sat = Math.tanh(dist / 6);
  return {
    x: (dx / dist) * sat * P.EYE_MAX,
    y: (dy / dist) * sat * P.EYE_MAX
  };
}

function getBlinkScale(t) {
  if (!blinkActive && t > nextBlinkT) {
    blinkActive = true;
    blinkStartT = t;
  }
  if (blinkActive) {
    const dt = t - blinkStartT;
    const half = P.BLINK_DUR / 2;
    if (dt < half) {
      return 1 - 0.94 * (dt / half);
    } else if (dt < P.BLINK_DUR) {
      return 0.06 + 0.94 * ((dt - half) / half);
    } else {
      blinkActive = false;
      nextBlinkT = t + P.BLINK_INTERVAL * (0.8 + Math.random() * 0.4);
    }
  }
  return 1;
}

function applyBlink(rect, base, scale) {
  const newH = Math.max(base.h * scale, 0.15);
  const newY = base.y + (base.h - newH) / 2;
  const newRx = Math.min(base.rx, newH / 2);
  rect.setAttribute('height', newH.toFixed(3));
  rect.setAttribute('y', newY.toFixed(3));
  rect.setAttribute('rx', newRx.toFixed(3));
  rect.setAttribute('ry', newRx.toFixed(3));
}

const startT = performance.now();
function loop(now) {
  const dt = (now - lastT) / 1000;
  lastT = now;
  const t = (now - startT) / 1000;

  /* 呼吸 scale */
  const phase = (Math.cos(2 * Math.PI * t / P.BREATH_PERIOD) + 1) / 2;
  const breathScale = P.SCALE_LOW + (1.0 - P.SCALE_LOW) * phase;

  /* 眼睛位置 */
  const target = computeEyeTarget();
  const eyeTime = mouseInside ? P.EYE_TRACK_TIME : P.RECENTER_TIME;
  const eyeRate = 1 - Math.exp(-dt / eyeTime);
  eyeOffset.x += (target.x - eyeOffset.x) * eyeRate;
  eyeOffset.y += (target.y - eyeOffset.y) * eyeRate;

  /* 云朵 rotate (eye.x 驱动 + lag) */
  const maxRot = P.MAX_ROT_DEG * Math.PI / 180;
  const targetRot = (eyeOffset.x / Math.max(P.EYE_MAX, 0.01)) * maxRot;
  cloudRotate += (targetRot - cloudRotate) * P.CLOUD_LAG;

  /* V2: 距离驱动 scale */
  let targetDistScale = 1.0;
  if (mouseInside) {
    const dx = mouseVB.x - FACE_CENTER.x;
    const dy = mouseVB.y - FACE_CENTER.y;
    const dist = Math.hypot(dx, dy);
    if (dist > P.DIST_NEAR) {
      const t01 = Math.min((dist - P.DIST_NEAR) / Math.max(P.DIST_FAR - P.DIST_NEAR, 0.01), 1);
      targetDistScale = 1.0 + (P.DIST_MAX_SCALE - 1.0) * t01;
    }
  }
  const distRate = 1 - Math.exp(-dt / P.DIST_RECENTER);
  curDistScale += (targetDistScale - curDistScale) * distRate;

  /* 应用 cloud transform — breath scale × distance scale 相乘 */
  const finalScale = breathScale * curDistScale;
  const angleDeg = cloudRotate * 180 / Math.PI;
  cloudGroup.setAttribute(
    'transform',
    `translate(12 12) rotate(${angleDeg.toFixed(3)}) scale(${finalScale.toFixed(4)}) translate(-12 -12)`
  );

  /* 眼睛 group transform */
  const eyeScale = curDistScale;
  eyeGroup.setAttribute(
    'transform',
    `translate(${eyeOffset.x.toFixed(3)} ${eyeOffset.y.toFixed(3)}) translate(12 12) scale(${eyeScale.toFixed(4)}) translate(-12 -12)`
  );

  /* 眨眼 */
  const blinkScale = getBlinkScale(t);
  applyBlink(eyeL, EYE_BASE.l, blinkScale);
  applyBlink(eyeR, EYE_BASE.r, blinkScale);

  /* 流动渐变 */
  const flowOffset = Math.sin(2 * Math.PI * t / P.FLOW_PERIOD) * P.FLOW_AMP;
  eyeGrad.setAttribute('y1', (3 + flowOffset).toFixed(2));
  eyeGrad.setAttribute('y2', (21 + flowOffset).toFixed(2));

  requestAnimationFrame(loop);
}

requestAnimationFrame(loop);
]]></script>
</svg>
